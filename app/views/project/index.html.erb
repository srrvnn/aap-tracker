<!-- Progress meters -->
<div class="row">
  <div class="span5" id="header-meter">
    <!-- In progress -->
    <h4> <i class=" icon-cogs"></i> Over all promises in progress: 20 out 70</h4>
    <div class="progress">
      <div class="progress-bar" role="progressbar" style="width: 30%;">
        15%
      </div>
    </div>
    <!-- Achieved -->
    <h4> <i class=" icon-ok"></i> Over all promises achieved: 10 out 70</h4>
    <div class="progress">
      <div class="progress-bar" role="progressbar" style="width: 15%;">
        15%
      </div>
    </div>
  </div>
</div>

<table class="table">
  <tr>
    <td>SNo</tb>
    <td>Title</tb>
    <td>Description</tb>
    <td>Status</tb>
    <% if user_signed_in? %>
      <td></td>
    <% end %>
  </tr>
<% @projects.each do |p| %>
  <tr onclick="location.href='<%= project_path(p.id) %>'">
    <td><%= p.sno %></td>
    <td><%= p.title %></td>
    <td><%= p.description %></td>
    <td><%= format_status(p.status) %></td>
    <% if user_signed_in? %>
      <td><a href="#" class="btn btn-danger" role="button">Delete</a></tb>
    <% end %>
  </tr>
<% end %>
</table>
